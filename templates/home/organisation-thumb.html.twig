        {# templates/home/modal.html.twig #}

 	
                  <!-- Thumb -->
                  {# @todo commonground schould be an config paramater #}
                  <a href="{{ path('app_organisation_view', {slug:organisation.slug}) }}">
                  <div class="col-md-4 col-sm-6 portfolio web panel" >
                    <div class="panel-body">
                      <div class="portfolio-title">
                        <div class="portfolio-icon">
                          <div><i class="fad fa-building"></i></div>
                        </div>
                        <div class="title">
                          <h4>{{ organisation.name }}</h4>
                          <em>{{ organisation.description }}</em>
                        </div>
                      </div>
                    </div>
                    <div class="portfolio-image">
                        <img src="{{ organisation.logo }}" alt="" />
                    </div>
                  </div>
                  </a>